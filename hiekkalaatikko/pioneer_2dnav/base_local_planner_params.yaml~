TrajectoryPlannerROS:
  max_vel_x: 0.5
  min_vel_x: 0.05
  max_rotational_vel: 1.5

  min_in_place_rotational_vel: 0.3

  backup_vel: -0.2

  # by default scoring is done in cells. when meter_scoring is true is's done
  # in meters. this affects to goal_distance and path_distance and gdist_scale
  # and pdist_scale parameters  
  meter_scoring: true  

  sim_time: 1.0

  #pdist_scale: 0.2
  #gdist_scale: 0.2
  
  path_distance_bias: 0.3
  goal_distance_bias: 0.3
  
  # more weight to avoiding obstacles. TODO: test if neccessary (simulation shows that it is...)
  #occdist_scale: 0.5
 
  # dwa (Dynamic Window ApproachDynamic Window Approach)is recommended is
  # acceleration limits are high enough, because it is computationally less
  # expensive than Trajectory Rollou
  dwa: false
 
  acc_lim_th: 1.5
  
  acc_lim_x: 1.5
  acc_lim_y: 1.5
  
  holonomic_robot: false
  
  yaw_goal_tolerance: 7.0
  xy_goal_tolerance: 0.3
